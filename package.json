{
  "name": "obio-radar-data",
  "version": "1.0.0",
  "description": "Content and Website for ObioRadar",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "node scripts/generateIndex.mjs && vitepress dev docs",
    "docs:build": "node scripts/generateIndex.mjs && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.2.3"
  }
}
