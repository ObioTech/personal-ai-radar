# LLM Provider — MVP chỉ hỗ trợ "gemini"
LLM_PROVIDER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# GitHub (optional, khuyến khích để tránh rate limit 60 req/hr)
GITHUB_TOKEN=

# Telegram (optional — nếu thiếu, tool vẫn chạy và chỉ tạo markdown)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
